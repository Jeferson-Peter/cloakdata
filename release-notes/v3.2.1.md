## Highlights

- Improved README clarity and coverage for the current `3.x` API surface
- Standardized several native-method error messages
- Aligned masking examples and preferred parameter naming around `mask_char`
- Removed internal-only docs from versioned project contents

## Quality Improvements

- Updated the README to better reflect:
  - the current built-in catalog
  - modern example configs
  - method selection guidance
  - the real development setup
- Standardized error-message prefixes across multiple native methods
- Kept masking parameter aliases compatible while documenting preferred usage
- Ignored internal-only local docs and generated demo output files from version control

## Compatibility Notes

- This is a patch release with no intended public API break
- `full_mask` now documents `mask_char` as the preferred parameter, while keeping `char` as a compatibility alias
- `mask_number` now documents `mask_char` as the preferred parameter, while keeping `mask` as a compatibility alias

## Install

```bash
pip install -U cloakdata
```

## Distribution

This release is published to PyPI. GitHub Release assets are not used as the distribution channel.
